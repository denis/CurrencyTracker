<p>
  <b>Name:</b>
  <%= @country.name %>
</p>

<p>
  <b>Code:</b>
  <%= @country.code %>
</p>

<p>
  <b>Currencies:</b>
  <ul>
    <% @country.currencies.each do |currency| %>
      <li><%= link_to currency.name, currency %></li>
    <% end %>
  </ul>
</p>

<p>
  <b>Status:</b>
  <%= @country.visited_by?(current_user) ? 'Visited' : 'Not Visited' %>
</p>

<% if @country.visited_by?(current_user) %>
  <%= button_to 'Unvisit', country_visit_path(@country), :method => 'delete' %>
<% else %>
  <%= button_to 'Visit', country_visit_path(@country) %>
<% end %>

<p>
  <%= link_to 'Back', countries_path %>
</p>
